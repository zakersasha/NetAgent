FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml ./
COPY src ./src

RUN pip install --no-cache-dir .

ENV PYTHONUNBUFFERED=1

CMD ["uvicorn", "xray_agent.app:app", "--host", "0.0.0.0", "--port", "8443", "--ssl-keyfile", "/certs/agent.key", "--ssl-certfile", "/certs/agent.crt"]
